# モジュール自動同期

YAGSL は、角度/ステアリング/アジマスモーターコントローラーの内部エンコーダーと絶対エンコーダーの差がデッドバンドを超えている場合に、モジュールが 0.5 秒間静止するたびに自動的に同期します。

この設定は `SwerveDrive.setModuleEncoderAutoSynchronize(bool, double)` で変更できます。第 1 引数は有効状態、第 2 引数はデッドバンド（度単位）です。
